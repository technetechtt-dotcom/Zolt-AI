import type { ZoltSkill } from "@zolt/capability-sdk";
export const curtailmentRiskSkill:ZoltSkill={id:"energy.curtailment-risk",version:"0.1.0",capabilityPack:"energy",supports:c=>typeof c.configuration.exportLimitKw==="number"&&typeof c.configuration.forecastPowerKw==="number",async analyse(context){const limit=Number(context.configuration.exportLimitKw),forecast=Number(context.configuration.forecastPowerKw),excess=Math.max(0,forecast-limit);if(excess<=0)return{warnings:[],noRecommendationReason:"Forecast below export limit"};const hours=Number(context.configuration.forecastWindowHours??1),energy=excess*hours;return{warnings:[],recommendation:{tenantId:context.tenantId,productId:context.productId,installationId:context.installationId,capabilityPack:"energy",skillId:"energy.curtailment-risk",type:"CURTAILMENT_RISK",title:"Forecast export constraint risk",summary:`Forecast output may exceed the export limit by ${excess.toFixed(1)} kW.`,proposedAction:"Review flexible-load or storage availability for the forecast window.",rationale:"Configured forecast production is above the site's configured export limit.",evidence:[{type:"metric",label:"Forecast power",value:forecast,unit:"kW"},{type:"limit",label:"Export limit",value:limit,unit:"kW"},{type:"estimate",label:"At-risk energy",value:energy,unit:"kWh"}],confidence:0.82,confidenceBreakdown:{configuration:1,forecast:0.8,dataQuality:0.75},severity:excess/limit>0.2?"HIGH":"MEDIUM",priority:80,assumptions:[`Forecast window ${hours}h`],uncertainties:["Actual irradiance may differ from forecast"],dataQualityWarnings:[],ruleVersion:"0.1.0",validFrom:context.analysisTime,expiresAt:new Date(Date.parse(context.analysisTime)+hours*3600_000).toISOString()}};}};
